# mongoose-lean-populate-virtual-issue
Demonstrates the failure of populate virtual fields.

## To run
* `npm install`
* `npm start`

## Expectation
The BFF field should be populated with a full user.

## Reality
The BFF field is `null`.
